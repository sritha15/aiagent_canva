import { Rows } from "@canva/app-ui-kit";
import { RemainingCredits } from "src/components";

export const Footer = () => {
  return (
    <Rows spacing="1u">
      <RemainingCredits />
    </Rows>
  );
};
